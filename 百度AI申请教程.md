# 百度 AI 颜值打分接口申请教程（5 分钟免费开通）

> 「系统打分」功能用的是**百度 AI 人脸检测**接口，能返回 0-100 的颜值分（beauty 字段）。
> 这个接口对个人免费，但需要申请一个 API Key 和 Secret Key，配置到「群主模式 → AI 打分接口配置」里。

---

## 第一步：注册登录百度智能云

1. 打开：**https://cloud.baidu.com/**
2. 点右上角「登录」，用**手机号/微信**登录（没账号会引导注册）
3. 首次使用需要**实名认证**（身份证，几分钟审核）

## 第二步：创建应用，获取 Key

1. 登录后打开人脸检测产品页：**https://console.bce.baidu.com/ai/#/ai/face/overview/index**
2. 点「**创建应用**」（或「立即使用」）
3. 填写：
   - **应用名称**：随便填，如 `xiangqin`
   - **应用类型**：选「个人开发者」或默认
   - **接口选择**：勾选「**人脸检测**」（Human Face Detect）
4. 创建成功后，页面上会显示：
   - **API Key**（一串字母数字）
   - **Secret Key**（一串字母数字）

## 第三步：领取免费额度（关键）

百度 AI 人脸检测有免费额度，但需要手动领取：

1. 在人脸检测产品页找「**领取免费资源**」或「**免费测试资源**」
2. 选择「人脸检测」→「领取」
3. 免费额度通常为：**每天 500 次**或每月一定次数，对相亲群够用了

## 第四步：填入你的工具

1. 打开相亲帮帮 H5 → 底部「我的」→ 底部「群主管理入口」→ 输入群主密码
2. 找到「**AI 打分接口配置**」
3. 把 **API Key** 和 **Secret Key** 分别粘贴进去
4. 点「保存 AI 配置」

配置完成后，成员上传照片点「开始系统打分」就会调用百度 AI 返回客观颜值分。

---

## 常见问题

**Q1：免费额度够用吗？**
百度人脸检测个人免费额度通常是每天 500 次。相亲群几十人打分，完全够用。超出后按次计费（很便宜），或第二天自动恢复。

**Q2：颜值分怎么算的？**
百度返回 0-100 的 beauty 分，工具自动除以 10 转成 0-10 分（保留 1 位小数）。这是 AI 根据人脸特征（对称性、五官比例等）客观评估的，不是主观打分。

**Q3：没配置 key 会怎样？**
成员点「开始系统打分」会提示「AI 打分接口未配置，请联系群主」。不影响其他功能，群主配置后即可使用。

**Q4：还有其他免费的颜值打分接口吗？**
有，比如旷视 Face++ 的 `beauty` 字段。但百度 AI 国内访问最快、申请最简单，推荐先用百度。

**Q5：隐私安全？**
照片只在用户本地压缩后，调用百度接口时上传给人脸检测（百度会处理但不存储，符合隐私政策）。群内分享的照片是压缩后的本地数据。
